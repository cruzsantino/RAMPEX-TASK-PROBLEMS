class Solution {
    public int findComplement(int n) {
       long mask=1;
       long num=n;
       while(mask<=num){
        mask<<=1;
       }
       mask-=1;
       long res=num^mask;
       return (int)res;
    }
}
